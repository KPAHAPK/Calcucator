<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.BaseButton" parent="TextAppearance.AppCompat.Button">
        <item name="android:gravity">center</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">@dimen/button</item>
        <item name="android:layout_margin">5dp</item>
        <item name="android:textColor">#ffffff</item>
    </style>

    <style name="Theme.NumberButton" parent="Theme.BaseButton">
        <item name="android:background">@drawable/btn_number_bg_selector</item>
    </style>

    <style name="Theme.NonNumber" parent="Theme.BaseButton">
        <item name="android:background">@drawable/btn_bg_selector</item>

    </style>

</resources>